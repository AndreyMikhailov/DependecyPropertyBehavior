DependecyPropertyBehavior
=========================

Helps to bind to properties in WPF that aren't dependency properties and allows to notify binding about changes through the optional specified event.
